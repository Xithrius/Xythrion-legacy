def index_days(lst):
    number_days = []
    days = ['m', 't', 'w', 'th', 'f']
    for i in lst:
        if i in days:
            days.index(i)
            number_days.append()
    return number_days
